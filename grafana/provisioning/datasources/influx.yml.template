apiVersion: 1
datasources:
  - name: InfluxDB
    type: influxdb
    url: http://influxdb:8086
    access: proxy
    jsonData:
      version: Flux
      organization: apiary
      defaultBucket: telemetry
    secureJsonData:
      token: ${INFLUX_INIT_TOKEN}